# Blog Automation 개선사항 체크리스트

> **최종 업데이트**: 2026-01-27
> **상세 분석**: [ANALYSIS-2026-01-27.md](./ANALYSIS-2026-01-27.md)

---

## 🔴 Critical (즉시 수정)

### 1. API 키 보안
- [ ] **백엔드 API 프록시 구현**
  - 경로: `/api/proxy/[provider]`
  - 모든 API 키를 서버에서만 관리

- [ ] **Google API URL 파라미터 제거**
  ```javascript
  // Before (취약)
  fetch(`https://...?key=${apiKey}`)

  // After (안전)
  fetch('/api/proxy/google', { method: 'POST' })
  ```

- [ ] **XOR 암호화 폐기**
  - 파일: `js/core/crypto.js`
  - HTTPS 환경 필수화
  - Dev 모드 암호화 제거

### 2. 입력값 검증
- [ ] **Prompt Injection 방지**
  ```javascript
  // js/services/blog-generator.js
  const safeTopic = topic.replace(/[`"'\\]/g, '\\$&');
  ```

---

## 🟡 High (1주일 내)

### 3. 접근성(a11y)
- [ ] 모든 `<button>`에 `aria-label` 추가
- [ ] 모든 `<input>`에 연결된 `<label>` 추가
- [ ] 포커스 트래핑 구현 (모달)
- [ ] 키보드 탐색 완성

### 4. 코드 분할
- [ ] `pages/settings.js` (1220줄) → 5개 파일로 분리
- [ ] `pages/write.js` (728줄) → 4개 파일로 분리
- [ ] `pages/batch.js` (537줄) → 3개 파일로 분리

### 5. 에러 처리
- [ ] 재시도 로직 구현 (지수 백오프)
- [ ] 구조화된 로깅 시스템

---

## 🟢 Medium (2주일 내)

### 6. 코드 품질
- [ ] 공통 레이아웃 함수 추출 (`ui/page-layout.js`)
- [ ] 상태 스키마 검증 추가
- [ ] 이벤트 리스너 자동 정리

### 7. 성능
- [ ] 동적 임포트 적용
- [ ] 불필요한 리렌더링 제거
- [ ] 이미지 lazy loading

---

## 🔵 Low (1개월 내)

### 8. 인증
- [ ] OAuth 2.0 네이버 블로그 연동
- [ ] 평문 비밀번호 저장 제거

### 9. 모니터링
- [ ] 에러 추적 대시보드
- [ ] 사용량 분석 차트
- [ ] 비용 예측 기능

---

## 파일별 수정 필요 사항

| 파일 | 수정 내용 | 우선순위 |
|------|----------|----------|
| `js/services/api-connection-manager.js` | URL 파라미터 API 키 제거 | 🔴 |
| `js/core/crypto.js` | XOR 암호화 제거 | 🔴 |
| `js/services/blog-generator.js` | 입력값 검증 추가 | 🔴 |
| `js/pages/settings.js` | 컴포넌트 분할 | 🟡 |
| `js/pages/write.js` | 컴포넌트 분할 | 🟡 |
| `js/ui/components.js` | aria-label 추가 | 🟡 |
| `js/core/state.js` | 스키마 검증 추가 | 🟢 |

---

## 테스트 체크리스트

### 배포 전 확인
- [ ] HTTPS 환경에서 암호화 동작 확인
- [ ] API 프록시 연결 테스트
- [ ] 키보드만으로 전체 기능 사용 가능
- [ ] Lighthouse 접근성 점수 > 80
- [ ] 콘솔에 API 키 노출 없음

---

*진행 상황은 이 문서에서 체크하세요.*
